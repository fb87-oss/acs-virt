# Samples

This directory contains runnable sample configs for `scripts/chiplets-launcher.py`.

```text
virt-axi-x64.toml  x86_64 microvm with virtio-blk and virtio-console virt-axi devices
virt-axi-a64.toml  AArch64 virt with virtio-blk and virtio-console virt-axi devices
```

The detailed schema and authoring guidelines live in:

```text
docs/runtime-config.md
docs/runtime-config.schema.json
```
